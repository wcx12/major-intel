# Major Intel 开发条例

## 代码注释要求

后续所有新增或修改的代码，都必须添加充分的解释性注释。注释的目标不是重复代码表面动作，而是让后来维护的人理解这段逻辑为什么存在、依赖什么数据口径、哪些边界不能越过。

必须重点注释以下内容：

- 模块入口：说明这个文件负责什么、不负责什么。
- 对外函数和类：说明输入、输出、主要副作用和失败场景。
- SQL 查询：说明关联字段、数据口径、为什么这样 JOIN 或过滤。
- 学校、专业、专业组等实体解析：说明匹配优先级、置信度来源和歧义处理方式。
- RAG、缓存、缺口队列、人工审核：说明命中/未命中后分别如何流转。
- 数据口径判断：说明哪些是学校级、专业通用级、校专业级、招生专业组级数据。
- 异常和保护逻辑：说明为什么不能继续生成答案，以及如何避免模型瞎编。
- 非显然算法或业务规则：说明推理依据、风险和后续可替换方案。

注释应当丰富但不堆砌。禁止只写机械注释，例如“给变量赋值”“调用函数”“返回结果”。如果一段代码本身已经很直白，优先把注释放在函数说明、SQL 说明或关键分支上。

修改旧代码时，如果行为、数据口径或异常路径发生变化，必须同步更新相关注释，不能让注释和实现脱节。

## 数据与安全

- 不允许把本地数据库密码、API Key、Cookie、Token 写进代码、文档或测试数据。
- 本地 MySQL 密码只能通过环境变量传入。
- SQL dump、批量抓取结果、缓存、日志和大体量报告默认不提交，除非明确用于样例或测试。

## 测试要求

新增功能必须配套测试，至少覆盖正常路径、边界路径和“不允许瞎编”的失败路径。涉及本地数据库查询的功能，应优先把数据解析、SQL 生成、结果渲染拆开测试，避免测试只能依赖某一次真实数据库状态。
